#include <stdio.h>
/**
 * main - Entry - Point
 *
 * Return: 0 (WelDONE)
 */

int main(void)
{
	char c;

	int b;

	c = 'a';
	while
		(b < 10) {
			putchar(b + '0');
			b++;
		}
	while
		(c <= 'f') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
