#include <stdio.h>
/**
 * main - Entry - point
 *
 * Return: Always 0 (WelDONE)
 */

int main(void)
{
	char v;


	v = 'z';
	while
		(v >= 'a') {
			putchar(v);
			v--;
		}
	putchar('\n');
	return (0);
}
